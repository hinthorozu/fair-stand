# 02 — Contract rolleri (DECISION-02 C, DECISION-03 A)

## moduleContracts

GOVERNANCE/spec. Production `itemBom` / `moduleBehavior` / `main` / `scene3d` / planner import etmez.  
`test/systemDevelopmentContract.test.js` katalog atamasını zorlar; runtime ikinci implementasyon yapılmadı.

## featureContracts

GOVERNANCE. `autoDepot.js` / `automaticWall.js` `FEATURE_CONTRACTS` okumaz. Testler planner çıktısını sözleşmeyle karşılaştırır.

## Sözleşme metni

`SYSTEM_DEVELOPMENT_CONTRACT.md` §2/§5/§6: registry runtime SoT değil. BOM runtime `itemBom.js`. Placement production `wallReflow.js`.
