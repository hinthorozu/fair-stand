# Validation boşlukları

`148` özellik için genel şema/validator yok (veya audit sütunu).

Kanıtlı istisnalar: `resolveItemBom` unit, `saveProject` project.id, haloColor hex regex, `getItem` unknown, illuminated-foam min width/height `Math.max`.

| id | not |
|---|---|
| `name` | validation yok (genel proje şeması yok). |
| `type` | validation yok (genel proje şeması yok). |
| `registry` | validation yok (genel proje şeması yok). |
| `static.bodyItems.glassShelfItemKey` | validation yok (genel proje şeması yok). |
| `static.bodyItems.horizontalItemKey` | validation yok (genel proje şeması yok). |
| `static.bodyItems.sideItemKey` | validation yok (genel proje şeması yok). |
| `static.composition.items` | validation yok (genel proje şeması yok). |
| `static.composition.moduleType` | validation yok (genel proje şeması yok). |
| `static.composition.nominalWidthCm` | validation yok (genel proje şeması yok). |
| `static.composition.options.shape` | validation yok (genel proje şeması yok). |
| `static.composition.options.shelfCount` | validation yok (genel proje şeması yok). |
| `static.connectorType` | validation yok (genel proje şeması yok). |
| `static.defaultColor` | validation yok (genel proje şeması yok). |
| `static.eyeCount` | validation yok (genel proje şeması yok). |
| `static.material` | validation yok (genel proje şeması yok). |
| `static.modelFile` | validation yok (genel proje şeması yok). |
| `static.modelRotationYDeg` | validation yok (genel proje şeması yok). |
| `static.nominalModuleWidthCm` | validation yok (genel proje şeması yok). |
| `static.paintable` | validation yok (genel proje şeması yok). |
| `static.panelRole` | validation yok (genel proje şeması yok). |
| `static.preserveModelScale` | validation yok (genel proje şeması yok). |
| `static.shape` | validation yok (genel proje şeması yok). |
| `static.shelfCount` | validation yok (genel proje şeması yok). |
| `static.sizeInch` | validation yok (genel proje şeması yok). |
| `static.stripOccupancy.align` | validation yok (genel proje şeması yok). |
| `static.stripOccupancy.stripCount` | validation yok (genel proje şeması yok). |
| `static.variant` | validation yok (genel proje şeması yok). |
| `static.videoWall.cols` | validation yok (genel proje şeması yok). |
| `static.videoWall.panelScreenHeightCm` | validation yok (genel proje şeması yok). |
| `static.videoWall.panelScreenWidthCm` | validation yok (genel proje şeması yok). |
| `static.videoWall.rows` | validation yok (genel proje şeması yok). |
| `static.visualRotationYDeg` | validation yok (genel proje şeması yok). |
| `catalog.present` | validation yok (tablo/audit). |
| `catalog.inKeysList` | validation yok (tablo/audit). |
| `catalog.groups` | validation yok (tablo/audit). |
| `catalog.depthCm` | validation yok (tablo/audit). |
| `catalog.eyeCount` | validation yok (tablo/audit). |
| `catalog.heightCm` | validation yok (tablo/audit). |
| `catalog.itemKey` | validation yok (tablo/audit). |
| `catalog.label` | validation yok (tablo/audit). |
| `catalog.modelFile` | validation yok (tablo/audit). |
| `catalog.modelRotationYDeg` | validation yok (tablo/audit). |
| `catalog.panelScreenHeightCm` | validation yok (tablo/audit). |
| `catalog.panelScreenWidthCm` | validation yok (tablo/audit). |
| `catalog.preserveModelScale` | validation yok (tablo/audit). |
| `catalog.screenHeightCm` | validation yok (tablo/audit). |
| `catalog.screenWidthCm` | validation yok (tablo/audit). |
| `catalog.shape` | validation yok (tablo/audit). |
| `catalog.shelfCount` | validation yok (tablo/audit). |
| `catalog.sizeInch` | validation yok (tablo/audit). |
| `catalog.stripOccupancy.align` | validation yok (tablo/audit). |
| `catalog.stripOccupancy.stripCount` | validation yok (tablo/audit). |
| `catalog.type` | validation yok (tablo/audit). |
| `catalog.unit` | validation yok (tablo/audit). |
| `catalog.variant` | validation yok (tablo/audit). |
| `catalog.videoWallCols` | validation yok (tablo/audit). |
| `catalog.videoWallRows` | validation yok (tablo/audit). |
| `catalog.visualRotationYDeg` | validation yok (tablo/audit). |
| `catalog.widthCm` | validation yok (tablo/audit). |
| `contract.present` | validation yok (tablo/audit). |
| `contract.profile` | validation yok (tablo/audit). |
| `contract.appearance.color` | validation yok (tablo/audit). |
| `contract.appearance.image` | validation yok (tablo/audit). |
| `contract.renderer.mode` | validation yok (tablo/audit). |
| `contract.runtime.mode` | validation yok (tablo/audit). |
| `contract.composition.mode` | validation yok (tablo/audit). |
| `contract.bom.mode` | validation yok (tablo/audit). |
| `contract.bom.source` | validation yok (tablo/audit). |
| `contract.bom.reason` | validation yok (tablo/audit). |
| `contract.state.owner` | validation yok (tablo/audit). |
| `contract.state.persistence` | validation yok (tablo/audit). |
| `capabilities.color` | validation yok (genel proje şeması yok). |
| `capabilities.image` | validation yok (genel proje şeması yok). |
| `capabilities.glass` | validation yok (genel proje şeması yok). |
| `capabilities.lightbox` | validation yok (genel proje şeması yok). |
| `capabilities.mesh` | validation yok (genel proje şeması yok). |
| `factory.fn` | validation yok (tablo/audit). |
| `factory.ok` | validation yok (tablo/audit). |
| `factory.reason` | validation yok (tablo/audit). |
| `factory.stateKeys` | validation yok (tablo/audit). |
| `state.bodySurface` | validation yok (genel proje şeması yok). |
| `state.chairCount` | validation yok (genel proje şeması yok). |
| `state.depthCm` | validation yok (genel proje şeması yok). |
| `state.eyeCount` | validation yok (genel proje şeması yok). |
| `state.faces` | validation yok (genel proje şeması yok). |
| `state.heightCm` | validation yok (genel proje şeması yok). |
| `state.imageAssetId` | validation yok (genel proje şeması yok). |
| `state.itemKey` | validation yok (genel proje şeması yok). |
| `state.modelFile` | validation yok (genel proje şeması yok). |
| `state.modelRotationYDeg` | validation yok (genel proje şeması yok). |
| `state.panelScreenHeightCm` | validation yok (genel proje şeması yok). |
| `state.panelScreenWidthCm` | validation yok (genel proje şeması yok). |
| `state.preserveModelScale` | validation yok (genel proje şeması yok). |
| `state.screenHeightCm` | validation yok (genel proje şeması yok). |
| `state.screenWidthCm` | validation yok (genel proje şeması yok). |
| `state.shape` | validation yok (genel proje şeması yok). |
| `state.shelfCount` | validation yok (genel proje şeması yok). |
| `state.shelfLightingOn` | validation yok (genel proje şeması yok). |
| `state.sizeInch` | validation yok (genel proje şeması yok). |
| `state.stripOccupancy` | validation yok (genel proje şeması yok). |
| `state.stripOccupancy.align` | validation yok (genel proje şeması yok). |
| `state.stripOccupancy.stripCount` | validation yok (genel proje şeması yok). |
| `state.type` | validation yok (genel proje şeması yok). |
| `state.videoWallCols` | validation yok (genel proje şeması yok). |
| `state.videoWallRows` | validation yok (genel proje şeması yok). |
| `state.visualRotationYDeg` | validation yok (genel proje şeması yok). |
| `state.wallGapCm` | validation yok (genel proje şeması yok). |
| `state.widthCm` | validation yok (genel proje şeması yok). |
| `bom.ok` | validation yok (tablo/audit). |
| `bom.error` | validation yok (tablo/audit). |
| `bom.lineCount` | validation yok (tablo/audit). |
| `bom.lines` | validation yok (tablo/audit). |
| `recipe.id` | validation yok (genel proje şeması yok). |
| `recipe.moduleType` | validation yok (genel proje şeması yok). |
| `recipe.connectionMode` | validation yok (genel proje şeması yok). |
| `recipe.innerCornerPanelItemKey` | validation yok (genel proje şeması yok). |
| `parents.recipe` | validation yok (genel proje şeması yok). |
| `parents.innerCorner` | validation yok (genel proje şeması yok). |
| `parents.replacement` | validation yok (genel proje şeması yok). |
| `parents.cluster` | validation yok (genel proje şeması yok). |
| `parents.showcaseBody` | validation yok (genel proje şeması yok). |
| `media.widthCm` | validation yok (genel proje şeması yok). |
| `media.screenWidthCm` | validation yok (genel proje şeması yok). |
| `media.screenHeightCm` | validation yok (genel proje şeması yok). |
| `media.videoWallRows` | validation yok (genel proje şeması yok). |
| `media.videoWallCols` | validation yok (genel proje şeması yok). |
| `profile.catalogWidthCm` | validation yok (genel proje şeması yok). |
| `renderer.fn` | validation yok (genel proje şeması yok). |
| `ui.sidebar` | validation yok (tablo/audit). |
| `ui.contextMenu.delete` | validation yok (tablo/audit). |
| `ui.contextMenu.duplicate` | validation yok (tablo/audit). |
| `ui.contextMenu.add` | validation yok (tablo/audit). |
| `ui.editable.color` | validation yok (tablo/audit). |
| `ui.editable.image` | validation yok (tablo/audit). |
| `ui.editable.glass` | validation yok (tablo/audit). |
| `ui.editable.fabric` | validation yok (tablo/audit). |
| `ui.editable.shelfLight` | validation yok (tablo/audit). |
| `ui.editable.foamResize` | validation yok (tablo/audit). |
| `ui.editable.floorSelect` | validation yok (tablo/audit). |
| `ui.editable.placement` | validation yok (tablo/audit). |
| `ui.editable.rotation` | validation yok (tablo/audit). |
| `persist.path` | validation yok (genel proje şeması yok). |
| `featureContracts` | validation yok (genel proje şeması yok). |
| `docs.definition` | validation yok (genel proje şeması yok). |
| `tests.srcHits` | validation yok (tablo/audit). |
| `tests.unitHits` | validation yok (tablo/audit). |
| `tests.e2eHits` | validation yok (tablo/audit). |
| `tests.docHits` | validation yok (tablo/audit). |
