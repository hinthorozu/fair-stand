# parke-acik — Kanonik Item

Migration öncesi tam envanter: [current-system/parke](../current-system/parke.md).
Sözleşme: [ITEM_CONTRACT](../contract/ITEM_CONTRACT.md).

## Kimlik
`FLOOR_ITEMS['parke-acik']`. `itemKey = parke-acik`. Name `Beyaz Meşe` (mevcut select metni). Type `floor`. Lamel `140 × 16 cm` (1.40 m × 0.16 m). `defaultColor=#e8dfd1`. `paintable=false`. Katalog/recipe/unit yok.

## Kalıcılık
Persist `stand.itemKey`. Eski `floorType` hydrate. Parke tek `parke` değeri değildir.
