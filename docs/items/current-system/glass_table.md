> Migration öncesi envanterdir; aktif canonical tanım `../definitions/glass_table.md` içindedir.

# glass_table — Mevcut Sistem Profili

Bağımsız katalog Item yoktu. Cam masa `furniture_table_chair_set_eames` renderer’ında procedural silindir olarak duruyordu.

## Kimlik / state (set içinden)

| Alan | Kod değeri |
|---|---|
| Parent catalog key | `furniture_table_chair_set_eames` |
| Çap | `75 cm` (`tableDiameterCm`) |
| Yükseklik | `74 cm` (`tableHeightCm`) |
| Renk | yok; cam masa sabittir |

Tabla / sap / taban renderer mesh’tir; ayrı Item değildir.
