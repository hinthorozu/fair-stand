{
  "name": "fair-stand",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "contract:verify": "node scripts/verify-change-contract.mjs",
    "test": "node --test",
    "build": "vite build",
    "preview": "vite preview",
    "e2e:deps": "npm install --no-save --no-package-lock @playwright/test@1.62.1",
    "e2e:install": "playwright install chromium",
    "e2e": "playwright test"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "three": "^0.184.0"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
