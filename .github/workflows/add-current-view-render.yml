name: Add Current View Render

on:
  push:
    branches: [ROG]
    paths:
      - 'scripts/add-current-view-render.mjs'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ROG
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/add-current-view-render.mjs
      - run: npm install
      - run: npm test
      - run: npm run build
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/scene3d.js src/main.js src/style.css index.html
          if git diff --cached --quiet; then
            echo "No changes"
          else
            git commit -m "Add current camera view rendering"
            git push origin ROG
          fi
