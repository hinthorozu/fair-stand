name: Lightbox Vivid

on:
  push:
    branches: [ROG]
    paths:
      - '.github/workflows/lightbox-vivid.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ROG
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Apply patch
        run: python3 scripts/patch_lightbox_vivid.py

      - name: Syntax check
        run: |
          node --check src/scene3d.js
          node --check test/lightboxFabricLighting.test.js

      - name: Install
        run: npm ci

      - name: Test
        run: npm test

      - name: Build
        run: npm run build

      - name: Commit and push
        shell: bash
        run: |
          rm -f .github/workflows/lightbox-vivid.yml scripts/patch_lightbox_vivid.py
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "tune: make lightbox fabric more vivid [skip ci]"
          git push origin ROG
