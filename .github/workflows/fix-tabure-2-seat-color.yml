name: Fix Tabure 2 Seat Color
on:
  push:
    branches: [ROG]
    paths: ['.github/workflows/fix-tabure-2-seat-color.yml']
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ROG
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: python .github/scripts/fix-tabure-2-seat-color.py
      - run: node --check src/scene3d.js
      - run: npm ci
      - run: npm test
      - run: npm run build
      - run: rm -f .github/scripts/fix-tabure-2-seat-color.py .github/workflows/fix-tabure-2-seat-color.yml
      - uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'fix: make Tabure 2 seat white and color-editable [skip ci]'
          branch: ROG
