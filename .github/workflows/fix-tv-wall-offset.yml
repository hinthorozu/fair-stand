name: Fix TV Wall Offset
on:
  push:
    branches: [ROG]
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ROG
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: python scripts/fix-tv-wall-offset.py
      - run: node --check src/scene3d.js
      - run: npm ci
      - run: npm test
      - run: npm run build
      - run: rm -f scripts/fix-tv-wall-offset.py .github/workflows/fix-tv-wall-offset.yml
      - uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "fix: move TV in front of wall surface [skip ci]"
