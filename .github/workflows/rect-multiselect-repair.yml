name: Rect Multiselect Repair

on:
  push:
    branches: [ROG]
    paths:
      - '.github/workflows/rect-multiselect-repair.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ROG
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Apply patch
        run: python scripts/patch-rect-multiselect.py
      - name: Syntax check
        run: |
          node --check src/scene3d.js
          node --check src/rectSelection.js
      - name: Install
        run: npm ci
      - name: Test
        run: npm test
      - name: Build
        run: npm run build
      - name: Commit and push
        run: |
          rm -f scripts/patch-rect-multiselect.py .github/workflows/rect-multiselect-repair.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix: restore rectangle ctrl panel selection [skip ci]'
          git push origin ROG
