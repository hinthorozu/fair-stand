name: Fix Top Fixture Air Drag

on:
  push:
    branches: [ROG]
    paths:
      - '.github/workflows/fix-top-fixture-air-drag.yml'

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ROG
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/fix-top-fixture-air-drag.mjs
      - run: npm install
      - run: npm test
      - run: npm run build
      - name: Commit changes
        run: |
          rm -f scripts/fix-top-fixture-air-drag.mjs .github/workflows/fix-top-fixture-air-drag.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Allow aerial LED floodlight dragging"
          git push origin ROG
