name: Fix counter grouped image once

on:
  push:
    branches: [ROG]
    paths:
      - '.github/workflows/fix-counter-rect-image-once.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ROG
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: python scripts/fix-counter-rect-image.py
      - run: npm test
      - run: npm run build
      - name: Commit and push
        run: |
          rm .github/workflows/fix-counter-rect-image-once.yml
          rm scripts/fix-counter-rect-image.py
          git config user.name "OpenAI"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/main.js .github/workflows/fix-counter-rect-image-once.yml scripts/fix-counter-rect-image.py
          git commit -m "Make counter images span selected panels"
          git push origin ROG
