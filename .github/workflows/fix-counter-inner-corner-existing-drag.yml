name: Fix Counter Inner Corner Existing Drag

on:
  push:
    branches: [ROG]
    paths:
      - scripts/fix-counter-inner-corner-existing-drag.mjs
      - .github/workflows/fix-counter-inner-corner-existing-drag.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ROG
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - run: node scripts/fix-counter-inner-corner-existing-drag.mjs
      - run: npm test
      - run: npm run build
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/scene3d.js
          git diff --cached --quiet || git commit -m "Enable banko corner snap while moving existing modules"
          git push origin ROG
