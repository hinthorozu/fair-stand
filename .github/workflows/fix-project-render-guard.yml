name: Fix Project Render Guard
on:
  push:
    branches: [ROG]
    paths: ['.github/workflows/fix-project-render-guard.yml']
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ROG
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: python .github/scripts/fix-project-render-guard.py
      - run: node --check src/scene3d.js
      - run: npm ci
      - run: npm test
      - run: npm run build
      - run: rm -f .github/scripts/fix-project-render-guard.py
      - uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'fix: guard project rendering against stale modules [skip ci]'
          branch: ROG
