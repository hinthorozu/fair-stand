name: Fix LED Placement

on:
  push:
    branches: [ROG]
    paths:
      - '.github/workflows/fix-led-placement.yml'

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ROG
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/fix-led-placement.mjs
      - run: npm install
      - run: npm test
      - run: npm run build
      - name: Commit changes
        run: |
          rm -f scripts/fix-led-placement.mjs .github/workflows/fix-led-placement.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix LED floodlight snap and duplication"
          git push origin ROG
