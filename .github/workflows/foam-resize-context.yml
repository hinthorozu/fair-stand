name: Foam Resize Context
on:
  push:
    branches: [ROG]
    paths: ['.github/workflows/foam-resize-context.yml']
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ROG
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node .github/scripts/foam-resize-context.cjs
      - run: node --check src/main.js
      - run: node --check src/moduleContextMenu.js
      - run: npm ci
      - run: npm test
      - run: npm run build
      - run: |
          rm -f .github/workflows/foam-resize-context.yml .github/scripts/foam-resize-context.cjs
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/main.js src/moduleContextMenu.js .github/workflows/foam-resize-context.yml .github/scripts/foam-resize-context.cjs
          git commit -m "Add right-click resize for illuminated foam"
          git push origin ROG
