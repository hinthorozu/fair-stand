name: Free Corner Ctrl Selection

on:
  push:
    branches: [ROG]
    paths:
      - '.github/workflows/free-corner-ctrl-selection.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ROG
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Apply patch
        run: python scripts/patch-free-corner-ctrl-selection.py
      - name: Syntax check
        run: |
          node --check src/scene3d.js
          node --check src/rectSelection.js
      - name: Install
        run: npm ci
      - name: Test
        run: npm test
      - name: Build
        run: npm run build
      - name: Commit and push
        run: |
          rm -f scripts/patch-free-corner-ctrl-selection.py .github/workflows/free-corner-ctrl-selection.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix: select connected free panel corners with ctrl [skip ci]'
          git push origin ROG
