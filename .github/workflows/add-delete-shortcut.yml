name: Add Delete Shortcut

on:
  push:
    branches: [ROG]
    paths:
      - 'scripts/add-delete-shortcut.mjs'
      - '.github/workflows/add-delete-shortcut.yml'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ROG
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node scripts/add-delete-shortcut.mjs
      - run: npm install
      - run: npm test
      - run: npm run build
      - name: Commit changes
        run: |
          if git diff --quiet; then
            echo "No changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/scene3d.js src/main.js
          git commit -m "Add Delete shortcut for selected modules"
          git push origin ROG
