name: Inspect Eames GLB
on:
  push:
    branches: [ROG]
    paths: ['.github/workflows/inspect-eames-glb.yml']
jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ROG
      - run: python .github/scripts/inspect-eames-glb.py
