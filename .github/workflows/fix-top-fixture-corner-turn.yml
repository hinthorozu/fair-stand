name: Fix Top Fixture Corner Turn

on:
  push:
    branches: [ROG]
    paths:
      - '.github/workflows/fix-top-fixture-corner-turn.yml'

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ROG
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/fix-top-fixture-corner-turn.mjs
      - run: npm install
      - run: npm test
      - run: npm run build
      - name: Commit changes
        run: |
          rm -f scripts/fix-top-fixture-corner-turn.mjs .github/workflows/fix-top-fixture-corner-turn.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Allow LED floodlights to turn corners"
          git push origin ROG
