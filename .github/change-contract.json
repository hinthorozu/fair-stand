{
  "schemaVersion": 2,
  "id": "f028-remove-illuminated-foam-on-feature-reset",
  "kind": "bugfix",
  "summary": "Make Tüm Özellikleri Kaldır delete illuminated-foam modules from the scene while resetting all remaining modules to their defaults with placement preserved.",
  "owners": [
    "src/main.js"
  ],
  "sourceOfTruth": [
    "src/main.js"
  ],
  "impact": {
    "catalog": "affected",
    "behavior": "affected",
    "state": "affected",
    "placement": "affected",
    "renderer": "affected",
    "persistence": "affected",
    "bom": "not-applicable",
    "ui": "affected",
    "composition": "affected",
    "assets": "affected",
    "storage": "affected",
    "importExport": "affected",
    "performance": "not-applicable",
    "accessibility": "not-applicable",
    "architecture": "affected",
    "security": "not-applicable",
    "tests": "affected"
  },
  "impactAnalysis": {
    "mode": "full-system",
    "affectedFiles": [
      "src/main.js"
    ],
    "affectedTests": [
      "e2e/f028-reset-features.spec.mjs",
      "e2e/smoke.spec.mjs",
      "test/f028FeatureReset.test.js",
      "test/illuminatedFoamModule.test.js",
      "test/systemImpactAnalysis.test.js"
    ],
    "affectedDocs": [
      "RELEASE_HARDENING_ROADMAP.md",
      "audit/FINDINGS.md",
      "audit/evidence/A10_UI_CONTROLS.md"
    ],
    "relatedFindings": {
      "affected": [
        "F-028"
      ],
      "reviewedNotAffected": [
        "F-010",
        "F-027",
        "F-039",
        "F-040"
      ]
    },
    "notes": "F-028 is intentionally narrow. During the existing global feature-reset action, illuminated-foam modules are removed instead of recreated; every other module still passes through the canonical createCatalogModuleState path with preservePlacement=true. The high-risk main.js governance domains are marked affected so the full-system gate reviews those boundaries, but no catalog dimensions, placement rules, composition rules, storage schema, archive format or architecture ownership are intentionally changed. The image asset itself remains in the project asset library, so only the scene module/reference is removed. Unknown module types still preserve the existing fail-closed reset behavior."
  },
  "tests": {
    "targeted": [
      "test/f028FeatureReset.test.js"
    ],
    "fullSuite": true,
    "build": true,
    "e2e": {
      "required": true,
      "targeted": [
        "e2e/f028-reset-features.spec.mjs"
      ],
      "reason": "The fix changes a browser-visible destructive reset action. Chromium verifies that illuminated foam is removed, the confirmation discloses that removal, and all other module type/width/placement values survive the reset and save path."
    }
  },
  "risk": {
    "level": "medium",
    "notes": "The action intentionally deletes illuminated-foam scene modules. Risk is limited to deleting the wrong module family or disturbing other module layout; targeted unit and browser regressions lock both boundaries."
  },
  "migration": {
    "required": false,
    "notes": "No IndexedDB version, project schema, module schema or ZIP archive schema changes are involved."
  },
  "rollback": "Restore the previous reset handler that maps every current module through createCatalogModuleState and remove the F-028 targeted regressions."
}
